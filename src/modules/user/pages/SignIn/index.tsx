import InputText from '@/shared/components/inputs/Text';

const SignIn = (): JSX.Element => {
  return (
    <main className="bg-gray-800 p-4 w-full min-h-screen">
      <InputText />
    </main>
  );
};

export default SignIn;
